let sum_list = xs ->
    let rec sum_list_aux = xs -> acc ->
      if xs == [] then acc
      else (sum_list_aux (rest xs) (acc + (first xs)))
    in
    sum_list_aux xs 0
in
sum_list [1, 2, 3, 4, 5]
